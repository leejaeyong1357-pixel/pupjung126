# 테크젠 법정의무교육 이수관리

성희롱 예방교육 / 장애인 인식개선교육 미이수자를 부서별로 추적하고,
각 부서 직책자에게 독려메일을 Outlook으로 일괄 발송하기 위한 도구입니다.

- 교육기간: **2026-09-14(월) ~ 2026-10-30(금)** (연내 전원 이수 필수)
- 관리 대상: **985명** (원본 명단 기준 미이수자)
  - 둘 다 미이수 849명 / 장애인만 미이수 136명

## 파일

| 파일 | 설명 |
|---|---|
| `테크젠_법정의무교육_이수관리.html` | **서무 담당자에게 배포하는 결과물.** 인터넷 없이 더블클릭으로 실행되는 단일 HTML |
| `build/build.py` | 원본 엑셀 → HTML 재생성 스크립트 |
| `build/part*.html`, `build/part*.js` | HTML 소스 조각 (CSS / 마크업 / 로직) |
| `build/source.xlsx` | 원본 명단 엑셀 |

## 재생성

```bash
pip install openpyxl
python3 build/build.py build/source.xlsx
```

원본 엑셀은 `학습자명단(장애인)`, `학습자명단(성희롱)` 두 시트를 사용하며
5행부터 `No / 이름 / 사번 / 이메일 / 연락처 / 상위부서 / 부서 / 직위` 순서를 가정합니다.
두 시트 모두 **미이수자 명단**이므로, 시트에 없으면 이수 처리됩니다.

## 화면 구성

1. **현황판** — 구분·이름·사번·직급·상위부서·부서·이메일·연락처·성희롱 이수여부·장애인 이수여부.
   구분/상위부서/부서/직위/직군/검색 필터, 정렬, 체크 선택, 엑셀 다운로드.
2. **부서별 진척** — 상위부서 이수율 막대 + 53개 부서(반/조) 상세. 행 클릭 시 현황판으로 이동.
3. **이수자 반영(주간)** — 교육원 이수자 명단의 사번을 붙여넣어 일괄 이수 처리.
   동명이인·미등록자는 경고로 분리, 직전 반영 취소 가능, 신규입사자 추가 지원.
4. **담당자·수신자 매핑** — 상위부서별 담당(서무) 지정, 부서별 수신자 자동 산정.
5. **독려메일 발송** — 발송 목록 생성 → ZIP(첨부 엑셀 + Outlook 발송 스크립트) 다운로드.
6. **사용법 / 체크리스트**

## 수신자 산정 규칙

- **관리직**(대표이사·상무·실장·팀장·책임매니저·매니저) → 본인 이메일 전원 수신
- **현장직** → **조장·반장·주임만** 수신, **기술사원 제외**(메일 확인 불가)
- 미이수자가 있는 **부서 단위**로 발송, **상위부서 담당(서무)**과 **팀장**은 참조(CC)
- 규칙은 화면에서 체크박스로 조정 가능하며, 이미 이수해 명단에 없는 조·반장은 직접 추가

## Outlook 발송 방식

브라우저는 보안상 첨부파일이 붙은 메일을 직접 보낼 수 없으므로,
Outlook COM을 호출하는 PowerShell 스크립트를 생성해 실행하는 방식을 씁니다.

1. ⑤탭에서 **발송 패키지(ZIP)** 다운로드
2. ZIP 압축 해제 (같은 폴더 유지)
3. `보내기_법정의무교육.ps1` 우클릭 → PowerShell에서 실행
   - 차단 시: `powershell -ExecutionPolicy Bypass -File .\보내기_법정의무교육.ps1`
4. Outlook 새 메일 창이 순서대로 열림 → 확인 후 보내기
   - 스크립트 상단 `$AUTO_SEND = $true` 로 바꾸면 확인 없이 즉시 발송

첨부 없이 보내려면 **Outlook 메일 창 열기(mailto)** 버튼을 사용합니다(본문에 명단 포함).

## 데이터 저장

원본 985명 명단은 HTML 파일에 내장되어 있고, **변경분(이수 처리·매핑·추가 인원)만**
브라우저 `localStorage`에 저장됩니다.

- 여러 명이 각자 열면 입력이 자동으로 합쳐지지 **않습니다**.
- 한 명이 입력 → **⤓ 백업**으로 JSON 저장 → 공유 → 나머지가 **⤒ 복원**.
- 브라우저 캐시 삭제·PC 교체 시 초기화되므로 **주 1회 백업 필수**.
