# 테크젠 법정의무교육 이수관리

성희롱 예방교육 / 장애인 인식개선교육 미이수자를 부서별로 추적하고,
각 부서 직책자에게 독려메일을 Outlook으로 일괄 발송하기 위한 도구입니다.

- 교육기간: **2026-09-14(월) ~ 2026-10-30(금)** (연내 전원 이수 필수)
- 관리 대상: **985명** (원본 명단 기준 미이수자)
  - 둘 다 미이수 849명 / 장애인만 미이수 136명

## 파일

| 파일 | 설명 |
|---|---|
| `테크젠_법정의무교육_이수관리.html` | **서무 담당자에게 배포하는 결과물.** 인터넷 없이 더블클릭으로 실행되는 단일 HTML |
| `build/build.py` | 원본 엑셀 → HTML 재생성 스크립트 |
| `build/part*.html`, `build/part*.js` | HTML 소스 조각 (CSS / 마크업 / 로직) |
| `build/source.xlsx` | 원본 명단 엑셀 |

## 재생성

```bash
pip install openpyxl
python3 build/build.py build/source.xlsx
```

원본 엑셀은 `학습자명단(장애인)`, `학습자명단(성희롱)` 두 시트를 사용하며
5행부터 `No / 이름 / 사번 / 이메일 / 연락처 / 상위부서 / 부서 / 직위` 순서를 가정합니다.
두 시트 모두 **미이수자 명단**이므로, 시트에 없으면 이수 처리됩니다.

## 화면 구성

1. **현황판** — 구분·이름·사번·직급·상위부서·부서·이메일·연락처·성희롱 이수여부·장애인 이수여부.
   구분/상위부서/부서/직위/직군/검색 필터, 정렬, 체크 선택, 엑셀 다운로드.
2. **부서별 진척** — 상위부서 이수율 막대 + 53개 부서(반/조) 상세. 행 클릭 시 현황판으로 이동.
3. **이수자 반영(주간)** — 교육원 이수자 명단의 사번을 붙여넣어 일괄 이수 처리.
   동명이인·미등록자는 경고로 분리, 직전 반영 취소 가능, 신규입사자 추가 지원.
4. **담당자·수신자 매핑** — 상위부서별 담당(서무) 지정, 부서별 수신자 자동 산정.
5. **독려메일 발송** — 발송 목록 생성 → ZIP(첨부 엑셀 + Outlook 발송 스크립트) 다운로드.
6. **사용법 / 체크리스트**

## 역할 분담

| 역할 | 담당 | 하는 일 |
|---|---|---|
| **관리자** | 미래성장팀 **이재용 매니저** (jason@teczen.kr) | ③탭에서 이수자 데이터 반영, 인원 보정, 백업 JSON 배포 |
| **서무 담당자** | 상위부서별 담당자 | **매주 월요일** 미이수자 확인 후 ⑤탭에서 독려메일 발송 |

서무는 ③탭(이수자 반영)을 사용하지 않습니다.

## 독려메일 라우팅 규칙

**Ⓐ 관리직 미이수자 → 1인 1통**
- 대상: 실장·팀장·책임매니저·매니저·상무·대표이사
- 받는사람 = 본인 / 참조 = 해당 팀장

**Ⓑ 현장직 미이수자 → 부서 1통**
- 대상: 기술사원·조장·반장·주임
- 받는사람 = 해당 부서 **조장·반장·주임** / 참조 = 해당 팀장
- **기술사원 본인에게는 발송하지 않습니다**(메일 확인 불가). 명단은 첨부·본문으로 전달
- 조장·반장이 없으면 → 팀장 → 담당 서무 순으로 자동 대체

「해당 팀장」은 ① 같은 부서의 팀장 ② 상위부서명과 같은 부서(팀 사무실)의 팀장 순으로 찾습니다.
그 위(실/전사) 팀장까지 올라가면 무관한 사람에게 발송되므로 거기서 멈춥니다.

### 원본 명단 기준 검증 결과

| 항목 | 결과 |
|---|---|
| 관리직 미이수자 | 107명 (전원 라우팅 가능) |
| 현장직 미이수자 | 878명 / 41개 부서 |
| 조장·반장 직접 수신 | 34개 부서 |
| 팀장으로 대체 | 6개 부서 (서산·평택파견, 구동검사반, 비즈니스솔루션팀) |
| 담당 서무로 대체 | 1개 부서 (노동조합) |
| **수신자 확보 불가** | **1개 부서 — 엔진조립2반(평택) 10명** |
| 기술사원이 수신자가 된 건 | 0건 |

엔진조립2반(평택)은 조장·반장이 이미 이수를 마쳐 미이수자 명단에 없습니다.
④탭에서 수신자를 직접 추가해야 메일이 나갑니다.

## Outlook 발송 방식 (CMD 전용, PowerShell 미사용)

사내 정책상 PowerShell을 쓸 수 없어, 배치파일 + Windows Script Host 경로를 사용합니다.

1. ⑤탭에서 **발송 패키지(ZIP)** 다운로드
2. ZIP **압축 해제** (ZIP 안에서 바로 실행하면 동작하지 않음)
3. **`보내기.bat` 더블클릭**
4. Outlook 새 메일 창이 순서대로 열림 → 확인 후 보내기
   - "검토 없이 즉시 발송"을 체크하고 내려받으면 자동 전송

bat이 막히면 CMD에서 `cscript //nologo send_mail.vbs`.
Windows Script Host까지 차단된 PC는 **mailto 버튼**(첨부 없이 본문에 명단 포함)을 사용합니다.

### 패키지 구성

| 파일 | 인코딩 | 설명 |
|---|---|---|
| `보내기.bat` | ASCII | 실행 파일 |
| `send_mail.vbs` | ASCII | Outlook COM 호출 로직 |
| `maillist.txt` | UTF-8 | 받는사람 / 참조 / 제목 / 첨부 / 본문파일 (탭 구분) |
| `본문_NN.htm` | UTF-8+BOM | 메일 본문 |
| `메일첨부_*.xlsx` | — | 부서별 미이수자 명단 |

한글 인코딩 문제를 피하기 위해 **bat과 vbs는 순수 ASCII로 유지**하고,
한글이 들어가는 수신자·제목·본문은 전부 UTF-8 외부 파일로 분리해 `ADODB.Stream`으로 읽습니다.

## 데이터 저장

원본 985명 명단은 HTML 파일에 내장되어 있고, **변경분(이수 처리·매핑·추가 인원)만**
브라우저 `localStorage`에 저장됩니다.

- 여러 명이 각자 열면 입력이 자동으로 합쳐지지 **않습니다**.
- 관리자가 반영 → **⤓ 백업**으로 JSON 저장 → 배포 → 서무가 **⤒ 복원**.
- 브라우저 캐시 삭제·PC 교체 시 초기화되므로 **주 1회 백업 필수**.
